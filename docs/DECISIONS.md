# Decisions

- Next.js App Router was chosen for fast full-stack route co-location.
- HTTP 418 is used as core satire and explicit policy signal.
- Seeded state is used for deterministic demos and reliable tests.
